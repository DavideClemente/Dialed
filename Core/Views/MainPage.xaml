<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AudioMixerWin.Core.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AudioMixerWin.Core.Controls"
    xmlns:vm="using:AudioMixerWin.Core.ViewModels"
    xmlns:loc="using:AudioMixerWin.Core.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,20,24,0" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page header -->
        <Grid Grid.Row="0" Margin="8,0">
            <StackPanel Spacing="2">
                <TextBlock Text="{loc:Loc Key=Nav_Mixer}" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="{loc:Loc Key=Mixer_Caption}" Style="{StaticResource SectionCaptionStyle}" />
            </StackPanel>

            <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{x:Bind ViewModel.AddChannelCommand}"
                    CornerRadius="8" BorderThickness="1" Padding="12,7">
                <StackPanel Orientation="Horizontal" Spacing="7">
                    <FontIcon Glyph="&#xE710;" FontSize="13" />
                    <TextBlock Text="{loc:Loc Key=Mixer_AddChannel}" FontSize="12" />
                </StackPanel>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1" Padding="0,0,0,24">
            <Grid>
                <ItemsRepeater x:Name="ChannelsRepeater" ItemsSource="{x:Bind ViewModel.Channels}">
                    <ItemsRepeater.Layout>
                        <UniformGridLayout MinItemWidth="260" MinItemHeight="200" ItemsStretch="Fill" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="vm:ChannelViewModel">
                            <Grid>
                                <controls:KnobCard Channel="{x:Bind}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </Grid>
        </ScrollViewer>

    </Grid>
</Page>
