<UserControl
    x:Class="AudioMixerWin.Core.Controls.KnobCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:AudioMixerWin.Core.Services"
    PointerPressed="OnPointerPressed"
    PointerMoved="OnPointerMoved"
    PointerReleased="OnPointerReleased"
    PointerCaptureLost="OnPointerCaptureLost">

    <Border CornerRadius="18" BorderThickness="1" Margin="8">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0.6,1">
                <GradientStop Color="#171717" Offset="0"/>
                <GradientStop Color="#101010" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <SolidColorBrush Color="#282828"/>
        </Border.BorderBrush>

        <Grid Opacity="{x:Bind ConvertOfflineToOpacity(Channel.IsOffline), Mode=OneWay}">

            <!-- Top white shimmer line -->
            <Border VerticalAlignment="Top" Height="1" CornerRadius="18,18,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00FFFFFF" Offset="0"/>
                        <GradientStop Color="#18FFFFFF" Offset="0.25"/>
                        <GradientStop Color="#18FFFFFF" Offset="0.75"/>
                        <GradientStop Color="#00FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <StackPanel Padding="18,16,18,18">

                <!-- Header: icon + name + settings -->
                <Grid>
                    <StackPanel Orientation="Horizontal" Spacing="11" VerticalAlignment="Center"
                                HorizontalAlignment="Left">

                        <!-- App icon container -->
                        <Border Width="40" Height="40" CornerRadius="10" Background="#1C1C1C"
                                BorderThickness="1">
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#2A2A2A"/>
                            </Border.BorderBrush>
                            <Grid>
                                <FontIcon Glyph="&#xE767;"
                                          FontSize="20"
                                          Foreground="#505050"
                                          Visibility="{x:Bind ConvertIconFallbackToVisibility(Channel.IconSource), Mode=OneWay}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Image Source="{x:Bind Channel.IconSource, Mode=OneWay}"
                                       Width="24" Height="24"
                                       Visibility="{x:Bind ConvertIconToVisibility(Channel.IconSource), Mode=OneWay}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Name + knob label badge -->
                        <StackPanel VerticalAlignment="Center" Spacing="5">
                            <TextBlock
                                Text="{x:Bind Channel.DisplayName, Mode=OneWay}"
                                FontSize="15"
                                FontWeight="SemiBold"
                                FontFamily="Segoe UI Variable Display"
                                Foreground="#F0F0F0"
                                CharacterSpacing="-15"/>
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Border Background="#141414" CornerRadius="4" Padding="6,2"
                                        HorizontalAlignment="Left">
                                    <TextBlock
                                        Text="{x:Bind Channel.KnobLabel, Mode=OneWay}"
                                        FontSize="9"
                                        FontFamily="Cascadia Code, Consolas"
                                        Foreground="#505050"
                                        CharacterSpacing="100"/>
                                </Border>
                                <Border Background="#1A1400" CornerRadius="4" Padding="5,2"
                                        Visibility="{x:Bind ConvertDisconnectedToVisibility(Channel.IsSerialConnected), Mode=OneWay}"
                                        ToolTipService.ToolTip="{loc:Loc Key=Knob_Offline_Tip}">
                                    <TextBlock
                                        Text="{loc:Loc Key=Knob_Offline}"
                                        FontSize="9"
                                        FontFamily="Cascadia Code, Consolas"
                                        Foreground="#B89530"/>
                                </Border>
                                <Border Background="#1A1414" CornerRadius="4" Padding="5,2"
                                        Visibility="{x:Bind ConvertOfflineToVisibility(Channel.IsOffline), Mode=OneWay}"
                                        ToolTipService.ToolTip="{loc:Loc Key=Knob_NotRunning_Tip}">
                                    <TextBlock
                                        Text="{loc:Loc Key=Knob_NotRunning}"
                                        FontSize="9"
                                        FontFamily="Cascadia Code, Consolas"
                                        Foreground="#C06060"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- Settings gear -->
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                            Click="OnSettingsClick"
                            Width="30" Height="30" Padding="0" CornerRadius="8" BorderThickness="1">
                        <Button.Resources>
                            <SolidColorBrush x:Key="ButtonBackground" Color="#141414"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#1E1E1E"/>
                            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#0A0A0A"/>
                            <SolidColorBrush x:Key="ButtonBorderBrush" Color="#282828"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#404040"/>
                            <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="#282828"/>
                            <SolidColorBrush x:Key="ButtonForeground" Color="#484848"/>
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="#888888"/>
                            <SolidColorBrush x:Key="ButtonForegroundPressed" Color="#484848"/>
                        </Button.Resources>
                        <TextBlock Text="⚙" FontSize="13"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>

                <!-- Volume slider -->
                <Slider
                    Value="{x:Bind Channel.Volume, Mode=TwoWay}"
                    Minimum="0"
                    Maximum="100"
                    Margin="0,22,0,0"
                    IsEnabled="{x:Bind ConvertOfflineToEnabled(Channel.IsOffline), Mode=OneWay}"
                    Opacity="{x:Bind ConvertMuteToOpacity(Channel.IsMuted), Mode=OneWay}">
                    <Slider.Resources>
                        <SolidColorBrush x:Key="SliderTrackFill" Color="#1C1C1C"/>
                        <SolidColorBrush x:Key="SliderTrackFillPointerOver" Color="#222222"/>
                        <SolidColorBrush x:Key="SliderTrackFillPressed" Color="#1C1C1C"/>
                        <SolidColorBrush x:Key="SliderTrackFillDisabled" Color="#141414"/>
                        <SolidColorBrush x:Key="SliderTrackValueFill" Color="#D8D8D8"/>
                        <SolidColorBrush x:Key="SliderTrackValueFillPointerOver" Color="#EEEEEE"/>
                        <SolidColorBrush x:Key="SliderTrackValueFillPressed" Color="#C0C0C0"/>
                        <SolidColorBrush x:Key="SliderTrackValueFillDisabled" Color="#1C1C1C"/>
                        <SolidColorBrush x:Key="SliderThumbBackground" Color="#FFFFFF"/>
                        <SolidColorBrush x:Key="SliderThumbBackgroundPointerOver" Color="#FFFFFF"/>
                        <SolidColorBrush x:Key="SliderThumbBackgroundPressed" Color="#D0D0D0"/>
                        <SolidColorBrush x:Key="SliderThumbBorderBrush" Color="Transparent"/>
                        <SolidColorBrush x:Key="SliderThumbBorderBrushPointerOver" Color="Transparent"/>
                        <SolidColorBrush x:Key="SliderThumbBorderBrushPressed" Color="Transparent"/>
                        <SolidColorBrush x:Key="SliderThumbBorderBrushDisabled" Color="Transparent"/>
                    </Slider.Resources>
                </Slider>

                <!-- Footer: mute button + volume % -->
                <Grid Margin="0,12,0,0">

                    <!-- Mute button: Border provides dynamic bg -->
                    <Border
                        HorizontalAlignment="Left"
                        Width="34" Height="34"
                        CornerRadius="17"
                        BorderThickness="1"
                        Background="{x:Bind GetMuteBrush(Channel.IsMuted), Mode=OneWay}"
                        BorderBrush="{x:Bind GetMuteBorderBrush(Channel.IsMuted), Mode=OneWay}">
                        <Button
                            Command="{x:Bind Channel.ToggleMuteCommand}"
                            Width="34" Height="34" Padding="0" CornerRadius="17" BorderThickness="0">
                            <Button.Resources>
                                <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#18FFFFFF"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#28000000"/>
                                <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent"/>
                            </Button.Resources>
                            <TextBlock
                                Text="{x:Bind FormatMuteIcon(Channel.IsMuted), Mode=OneWay}"
                                FontSize="15"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Button>
                    </Border>

                    <!-- Volume percentage -->
                    <TextBlock
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Text="{x:Bind FormatPercent(Channel.Volume), Mode=OneWay}"
                        FontSize="13"
                        FontFamily="Cascadia Code, Consolas"
                        FontWeight="SemiBold"
                        Foreground="#505050"/>
                </Grid>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>
