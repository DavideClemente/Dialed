<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AudioMixerWin.Core.Controls.AppPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:AudioMixerWin.Core.Models"
    xmlns:loc="using:AudioMixerWin.Core.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{loc:Loc Key=AppPicker_Title}"
    PrimaryButtonText="{loc:Loc Key=AppPicker_Select}"
    SecondaryButtonText="{loc:Loc Key=AppPicker_Remove}"
    CloseButtonText="{loc:Loc Key=Common_Cancel}"
    RequestedTheme="Dark">

    <StackPanel Spacing="8">
        <InfoBar x:Name="HideInfoBar" Severity="Warning" IsOpen="False" IsClosable="True" />

        <TextBlock Text="{loc:Loc Key=AppPicker_Knob}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
        <ComboBox x:Name="KnobCombo" ItemsSource="{x:Bind KnobOptions}"
                  DisplayMemberPath="Label" HorizontalAlignment="Stretch" />

        <TextBlock Text="{loc:Loc Key=AppPicker_App}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
        <ListView x:Name="SessionsList" ItemsSource="{x:Bind SelectableSessions}" SelectionMode="Single"
                  Background="Transparent">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="CornerRadius" Value="6"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:AudioSession">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid Width="20" Height="20" VerticalAlignment="Center">
                            <FontIcon Glyph="&#xE767;" FontSize="14"
                                      Foreground="{StaticResource TextDisabledBrush}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Visibility="{x:Bind IconSource, Converter={StaticResource NullToVisible}}"/>
                            <Image Source="{x:Bind IconSource}" Width="20" Height="20"
                                   Stretch="UniformToFill"
                                   Visibility="{x:Bind IconSource, Converter={StaticResource NullToCollapsed}}"/>
                        </Grid>
                        <TextBlock Grid.Column="1" Text="{x:Bind DisplayName}" VerticalAlignment="Center"
                                   FontSize="13" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <Button Grid.Column="2" Content="{loc:Loc Key=Common_Hide}" Tag="{x:Bind}" Click="OnHideClick"
                                Padding="10,4" CornerRadius="6" BorderThickness="1" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
